import { remoteComponent } from '../../../helpers/remote-component';
import { module, test } from 'qunit';

module('Unit | Helper | remote component');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = remoteComponent(42);
  assert.ok(result);
});
